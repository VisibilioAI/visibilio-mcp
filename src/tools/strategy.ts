import { stubTool, type ToolDescriptor } from './_define.js';

export const strategyTools: readonly ToolDescriptor[] = [
  stubTool('generate_strategy'),
  stubTool('generate_personas'),
  stubTool('research_market'),
];
